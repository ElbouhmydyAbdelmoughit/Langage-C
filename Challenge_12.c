#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main()
{
    int nbr,temp;

    printf("Entrez votre nombre \n");
    scanf("%d",&nbr);
    nbr[0] = temp;
    temp = nbr[2];
    nbr[0] = temp;

    printf("le ruseltat est :%d",nbr);

    return 0;
}